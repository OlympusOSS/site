---
title: "CI — octl"
description: "CI workflow in octl/.github/workflows/ci.yml"
---

# CI — octl

**Repo:** `octl`

**File:** [`octl/.github/workflows/ci.yml`](https://github.com/OlympusOSS/octl/blob/main/.github/workflows/ci.yml)

**Triggers:** `push` (branches: main), `pull_request`

## Jobs

- **`test`** — 7 steps, runs on `ubuntu-latest`

### Job: `test`

Runs on: `ubuntu-latest`

Steps:

- `actions/checkout@v6`
- `actions/setup-node@v6`
- `npm ci`
- `npx biome ci`
- `npx tsc --noEmit`
- `npm test`
- `npx tsc`


## Source

```yaml
name: CI — octl

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: 'npm'
      - run: npm ci
      - run: npx biome ci
      - run: npx tsc --noEmit
      - run: npm test
      - run: npx tsc

```

---

*Generated from `octl/.github/workflows/ci.yml` at build time.*
