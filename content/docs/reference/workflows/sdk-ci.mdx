---
title: "CI — SDK"
description: "CI workflow in sdk/.github/workflows/ci.yml"
---

# CI — SDK

**Repo:** `sdk`

**File:** [`sdk/.github/workflows/ci.yml`](https://github.com/OlympusOSS/sdk/blob/main/.github/workflows/ci.yml)

**Triggers:** `push` (branches: main), `pull_request`, `workflow_dispatch`

## Jobs

- **`test`** — 4 steps, runs on `ubuntu-latest`
- **`publish`** — 3 steps, runs on `ubuntu-latest`

### Job: `test`

Runs on: `ubuntu-latest`

Steps:

- actions/checkout@v6
- oven-sh/setup-bun@v2
- bun install
- bunx tsc --noEmit

### Job: `publish`

Runs on: `ubuntu-latest`

Steps:

- actions/checkout@v6
- actions/setup-node@v6
- npm publish --access public --provenance


## Source

```yaml
name: CI — SDK

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - run: bun install
      - run: bunx tsc --noEmit

  publish:
    needs: test
    if: startsWith(github.ref, 'refs/tags/v') || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'
      - run: npm publish --access public --provenance
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

```

---

*Generated from `sdk/.github/workflows/ci.yml` at build time.*
