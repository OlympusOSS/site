---
title: "CI — canvas"
description: "CI workflow in canvas/.github/workflows/ci.yml"
---

# CI — canvas

**Repo:** `canvas`

**File:** [`canvas/.github/workflows/ci.yml`](https://github.com/OlympusOSS/canvas/blob/main/.github/workflows/ci.yml)

**Triggers:** `push` (branches: main), `pull_request`

## Jobs

- **`test`** — 9 steps, runs on `ubuntu-latest`

### Job: `test`

Runs on: `ubuntu-latest`

Steps:

- `actions/checkout@v6`
- `oven-sh/setup-bun@v2`
- `bun install --frozen-lockfile`
- `Lint`
- `Typecheck`
- `Run vitest`
- `Snapshot coverage gate (100%)`
- `Coverage`
- `Bundle size`


## Source

```yaml
name: CI — canvas

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - run: bun install --frozen-lockfile

      - name: Lint
        run: bun run lint

      - name: Typecheck
        run: bun run typecheck

      - name: Run vitest
        run: bun run test

      - name: Snapshot coverage gate (100%)
        run: bun run snapshot-coverage

      - name: Coverage
        run: bun run test:coverage

      - name: Bundle size
        run: bun run size

```

---

*Generated from `canvas/.github/workflows/ci.yml` at build time.*
