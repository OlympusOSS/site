---
title: "Claude Code"
description: "CI workflow in athena/.github/workflows/claude.yml"
---

# Claude Code

**Repo:** `athena`

**File:** [`athena/.github/workflows/claude.yml`](https://github.com/OlympusOSS/athena/blob/main/.github/workflows/claude.yml)

**Triggers:** `issue_comment`, `pull_request_review_comment`, `issues`, `pull_request_review`

## Jobs

- **`claude`** — 2 steps, runs on `ubuntu-latest`

### Job: `claude`

Runs on: `ubuntu-latest`

Steps:

- actions/checkout@v4
- anthropics/claude-code-action@v1


## Source

```yaml
name: Claude Code

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  issues:
    types: [opened, assigned, labeled]
  pull_request_review:
    types: [submitted]

jobs:
  claude:
    if: |
      (github.event_name == 'issue_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'pull_request_review_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'issues') ||
      (github.event_name == 'pull_request_review')
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      issues: write
      id-token: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}

```

---

*Generated from `athena/.github/workflows/claude.yml` at build time.*
