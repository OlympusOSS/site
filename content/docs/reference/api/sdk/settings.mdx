---
title: settings
description: "SDK module settings, exports and signatures"
---

**Source:** `sdk/src/settings.ts`

For implementation details and inline JSDoc, see the source on GitHub: [`sdk/src/settings.ts`](https://github.com/OlympusOSS/sdk/blob/main/src/settings.ts).

## Exports

### `Setting` (`interface`)

```ts
interface Setting
```

### `SetSettingOptions` (`interface`)

```ts
interface SetSettingOptions
```

### `getSetting` (`function`)

```ts
function getSetting (key:
```

### `getSettingOrDefault` (`function`)

```ts
function getSettingOrDefault (
```

### `getSecretSetting` (`function`)

```ts
function getSecretSetting (key:
```

### `setSetting` (`function`)

```ts
function setSetting (
```

### `deleteSetting` (`function`)

```ts
function deleteSetting (key:
```

### `listSettings` (`function`)

```ts
function listSettings (category?:
```

### `listSettingsForDisplay` (`function`)

```ts
function listSettingsForDisplay (
```

### `BatchSettingEntry` (`interface`)

```ts
interface BatchSettingEntry
```

### `batchSetSettings` (`function`)

```ts
function batchSetSettings (
```

---

*Generated from `sdk/src/settings.ts` at build time.*
