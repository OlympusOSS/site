---
title: locations
description: "SDK module locations, exports and signatures"
---

**Source:** `sdk/src/locations.ts`

For implementation details and inline JSDoc, see the source on GitHub: [`sdk/src/locations.ts`](https://github.com/OlympusOSS/sdk/blob/main/src/locations.ts).

## Exports

### `SessionLocation` (`interface`)

```ts
interface SessionLocation
```

### `AddSessionLocationData` (`interface`)

```ts
interface AddSessionLocationData
```

### `GetSessionLocationsOptions` (`interface`)

```ts
interface GetSessionLocationsOptions
```

### `addSessionLocation` (`function`)

```ts
function addSessionLocation (data:
```

### `cleanupOldLocations` (`function`)

```ts
function cleanupOldLocations (retentionDays:
```

### `getSessionLocations` (`function`)

```ts
function getSessionLocations (options?:
```

---

*Generated from `sdk/src/locations.ts` at build time.*
