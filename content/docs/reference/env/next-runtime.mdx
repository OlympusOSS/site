---
title: "NEXT_RUNTIME"
description: "Environment variable used by: athena, hera"
---

# `NEXT_RUNTIME`

**Read by:** `athena`, `hera`

**Sensitive:** No — safe to log / share.

**Required:** unspecified

## Where it's read

- **athena** — `grep -r "NEXT_RUNTIME" ../athena/src` for code locations
- **hera** — `grep -r "NEXT_RUNTIME" ../hera/src` for code locations

## Setting it

### Dev

Set in your local `.env.dev` file (managed by `octl deploy`).

### Prod

```bash
gh secret set NEXT_RUNTIME --body "<value>" -R OlympusOSS/platform
gh workflow run deploy.yml
```


---

*Generated from source by `scripts/gen/env-vars-per-var.mjs`.*
