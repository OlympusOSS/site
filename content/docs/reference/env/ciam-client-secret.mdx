---
title: "CIAM_CLIENT_SECRET"
description: "Environment variable used by: site"
---

# `CIAM_CLIENT_SECRET`

**Read by:** `site`

**Sensitive:** No — safe to log / share.

**Required:** unspecified

## Where it's read

- **site** — `grep -r "CIAM_CLIENT_SECRET" ../site/src` for code locations

## Setting it

### Dev

Set in your local `.env.dev` file (managed by `octl deploy`).

### Prod

```bash
gh secret set CIAM_CLIENT_SECRET --body "<value>" -R OlympusOSS/platform
gh workflow run deploy.yml
```


---

*Generated from source by `scripts/gen/env-vars-per-var.mjs`.*
