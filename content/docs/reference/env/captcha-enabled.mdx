---
title: "CAPTCHA_ENABLED"
description: "Environment variable used by: athena, hera"
---

# `CAPTCHA_ENABLED`

**Read by:** `athena`, `hera`

**Sensitive:** No — safe to log / share.

**Required:** unspecified

## Where it's read

- **athena** — `grep -r "CAPTCHA_ENABLED" ../athena/src` for code locations
- **hera** — `grep -r "CAPTCHA_ENABLED" ../hera/src` for code locations

## Setting it

### Dev

Set in your local `.env.dev` file (managed by `octl deploy`).

### Prod

```bash
gh secret set CAPTCHA_ENABLED --body "<value>" -R OlympusOSS/platform
gh workflow run deploy.yml
```


---

*Generated from source by `scripts/gen/env-vars-per-var.mjs`.*
