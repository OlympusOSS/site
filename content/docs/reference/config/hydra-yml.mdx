---
title: hydra.yml
description: "Field-by-field reference for every Hydra configuration file across CIAM/IAM and dev/prod"
---

Olympus runs **four** Hydra instances. Each has its own `hydra.yml` config file:

- **CIAM dev** — `platform/dev/ciam-hydra/hydra.yml`
- **CIAM prod** — `platform/prod/ciam-hydra/hydra.yml`
- **IAM dev** — `platform/dev/iam-hydra/hydra.yml`
- **IAM prod** — `platform/prod/iam-hydra/hydra.yml`

> The full Hydra configuration schema is documented in [Ory's reference](https://www.ory.sh/docs/hydra/reference/configuration). This page documents the **Olympus-specific** values.

## `oidc`

| Key | CIAM dev | CIAM prod | IAM dev | IAM prod |
|-----|----------|-----------|---------|----------|
| `subject_identifiers.pairwise.salt` | `ciam-hydra-pairwise-salt` | — | `iam-hydra-pairwise-salt` | — |
| `subject_identifiers.supported_types` | array[2] | array[2] | array[2] | array[2] |

## `secrets`

| Key | CIAM dev | CIAM prod | IAM dev | IAM prod |
|-----|----------|-----------|---------|----------|
| `system` | array[1] | — | array[1] | — |

## `serve`

| Key | CIAM dev | CIAM prod | IAM dev | IAM prod |
|-----|----------|-----------|---------|----------|
| `admin.port` | `5003` | — | `7003` | — |
| `cookies.same_site_mode` | `Lax` | `Lax` | `Lax` | `Lax` |
| `public.port` | `5002` | — | `7002` | — |

## `urls`

| Key | CIAM dev | CIAM prod | IAM dev | IAM prod |
|-----|----------|-----------|---------|----------|
| `consent` | `http://localhost:3000/consent` | — | `http://localhost:4000/consent` | — |
| `login` | `http://localhost:3000/login` | — | `http://localhost:4000/login` | — |
| `logout` | `http://localhost:3000/logout` | — | `http://localhost:4000/logout` | — |
| `self.issuer` | `http://localhost:3102` | — | `http://localhost:4102` | — |

---

*Generated from the four `hydra.yml` files at build time.*
